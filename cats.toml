[pool]
poll_interval = "5s"
max_retries = 2

# Worktrees directory (default: /tmp/cats-worktrees)
# Set CATS_WORKTREES_DIR env var or override in topic.sh
# worktrees_dir = "/tmp/cats-worktrees"

[sandbox]
gpu = false
network = true
extra_ro = []
extra_rw = []
